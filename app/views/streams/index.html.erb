<br />
<% @streams.each do |stream| %>
  <div>
    <p><b><h5><%= link_to stream.name, show_stream_url %></h5></b></p> <br />
    <p><%= stream.description %></p>
  </div>
  <hr />
<% end %>

<br />

<!-- <%= button_to("Logout", logout_path , method: :delete) %> -->
